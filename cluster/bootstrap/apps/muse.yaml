apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: muse
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
  - chart: app-template
    repoURL: https://bjw-s.github.io/helm-charts/
    targetRevision: x
    helm:
      valueFiles:
      - $repo/cluster/values/apps/muse/values.yaml
      releaseName: muse
  - repoURL: git@github.com:narwhal99/argocd.git
    path: cluster/values/apps/muse
    targetRevision: HEAD
    ref: repo
  destination:
    name: in-cluster
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
    - RespectIgnoreDifferences=true
  # ignoreDifferences:
  # - kind: PersistentVolume
  #   jsonPointers:
  #   - /spec/claimRef/resourceVersion
  #   - /spec/claimRef/uid
  #   - /status/lastPhaseTransitionTime
  # - group: apps
  #   kind: StatefulSet
  #   jsonPointers:
  #   - /spec/volumeClaimTemplates
  ignoreDifferences:
    - group: external-secrets.io
      kind: ExternalSecret
      jsonPointers:
        - /spec/data/*/remoteRef/conversionStrategy
        - /spec/data/*/remoteRef/decodingStrategy
        - /spec/data/*/remoteRef/metadataPolicy