apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: '0'
spec:
  project: default
  sources:
  - chart: argo-cd
    repoURL: https://argoproj.github.io/argo-helm
    targetRevision: 9.4.2
    helm:
      valueFiles:
        - $repo/cluster/values/infrastructure/argo-cd/values.yaml
  - repoURL: git@github.com:narwhal99/argocd.git
    path: cluster/values/infrastructure/argo-cd/
    targetRevision: master
    ref: repo
  destination:
    name: in-cluster
    namespace: argocd
  syncPolicy:
    automated:
      selfHeal: true
      prune: false
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
