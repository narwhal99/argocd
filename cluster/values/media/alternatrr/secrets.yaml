apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: alternatrr-secret
spec:
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  data:
    - secretKey: username
      remoteRef:
        key: kv/apps/alternatrr
        property: oidc_client_id
    - secretKey: password
      remoteRef:
        key: kv/apps/alternatrr
        property: oidc_client_secret
