apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-server
  namespace: media
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 150Mi
