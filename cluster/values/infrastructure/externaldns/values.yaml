domainFilters:
  - raymol.com
provider: rfc2136

extraArgs:
  - --rfc2136-host=10.20.0.4
  - --rfc2136-port=53
  - --rfc2136-zone=raymol.com
  - --rfc2136-min-ttl=0s
  - --rfc2136-tsig-axfr
  - --rfc2136-tsig-secret-alg=hmac-sha512
  - --rfc2136-tsig-keyname=externaldns

env:
  - name: EXTERNAL_DNS_RFC2136_TSIG_SECRET
    valueFrom:
      secretKeyRef:
        name: tsig-secret
        key: rfc2136_tsig_secret

serviceMonitor:
  enabled: true

policy: sync

sources:
  - service
  - ingress
  - gateway-grpcroute
  - gateway-httproute