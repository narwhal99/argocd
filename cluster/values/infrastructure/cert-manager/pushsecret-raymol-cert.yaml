apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: sync-wildcard-raymol-cert-to-vault
  namespace: cert-manager
spec:
  updatePolicy: Replace
  secretStoreRefs:
    - name: vault
      kind: ClusterSecretStore
  selector:
    secret:
      name: wildcard-raymol-tls
  data:
    - match:
        secretKey: tls.crt
        remoteRef:
          remoteKey: raymol.com/wildcard-raymol-tls
          property: tls-cert
    - match:
        secretKey: tls.key
        remoteRef:
          remoteKey: raymol.com/wildcard-raymol-tls
          property: tls-key