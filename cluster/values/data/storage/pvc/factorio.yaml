apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: factorio-conf
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
