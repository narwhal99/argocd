apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: harbor
  namespace: harbor
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 100Gi
