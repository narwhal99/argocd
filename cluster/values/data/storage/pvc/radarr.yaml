apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-conf
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
