apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mediabox.fullname" . }}-test-connection"
  labels:
    {{- include "mediabox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mediabox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
