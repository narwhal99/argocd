apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediabox.fullname" . }}
  labels:
    {{- include "mediabox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mediabox.selectorLabels" . | nindent 4 }}
