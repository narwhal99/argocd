apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: qbittorrent-{{ .Values.spec.destination.cluster }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .Values.spec.destination.namespace }}
    server: {{ .Values.spec.destination.server }}
  syncPolicy:
    automated: { }
  project: default
  sources:
    - repoURL: https://bjw-s.github.io/helm-charts/
      chart: app-template
      targetRevision: x
      helm:
        valueFiles:
          - $values/values/{{ .Values.spec.destination.cluster }}/mediabox/qbittorrent.yaml
        valuesObject:
          global:
            labels:
              stage: {{ .Values.spec.destination.cluster }}    
            env: {{ .Values.spec.destination.cluster }}
    - repoURL: {{ .Values.spec.source.repoURL }}
      targetRevision: {{ .Values.spec.source.targetRevision }}
      ref: values
      # sources:
      #   - repoURL: https://bjw-s.github.io/helm-charts/
      #     chart: app-template
      #     targetRevision: x
      #     helm:
      #       valueFiles:
      #         - $values/values/{{ .namespace }}/mediabox/sonarr.yaml
      #         - $values/values/{{ .namespace }}/mediabox/qbittorrent.yaml
      #   - repoURL: git@github.com:narwhal99/argocd.git
      #     targetRevision: HEAD
      #     ref: values
