apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sonarr-{{ .Values.spec.destination.namespace }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .Values.spec.destination.namespace }}
    server: {{ .Values.spec.destination.server }}
  syncPolicy:
    automated: { }
  project: default
  sources:
    - repoURL: https://bjw-s.github.io/helm-charts/
      chart: app-template
      targetRevision: x
      helm:
        valueFiles:
          - $values/values/{{ .namespace }}/mediabox/sonarr.yaml
        valuesObject:
          global:
            labels:
              stage: {{ .Values.spec.destination.namespace }}    
            env: {{ .Values.spec.destination.namespace }}
    - repoURL: {{ .Values.spec.destination.namespace }}
      targetRevision: {{ .Values.spec.destination.targetRevision }}
      ref: values
      # sources:
      #   - repoURL: https://bjw-s.github.io/helm-charts/
      #     chart: app-template
      #     targetRevision: x
      #     helm:
      #       valueFiles:
      #         - $values/values/{{ .namespace }}/mediabox/sonarr.yaml
      #         - $values/values/{{ .namespace }}/mediabox/qbittorrent.yaml
      #   - repoURL: git@github.com:narwhal99/argocd.git
      #     targetRevision: HEAD
      #     ref: values
