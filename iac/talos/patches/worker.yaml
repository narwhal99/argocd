machine:
  kubelet:
    extraMounts:
      - destination: /var/longhorn
        type: bind
        source: /var/longhorn
        options:
          - bind
          - rshared
          - rw
  disks:
    - device: /dev/sdb
      partitions:
        - mountpoint: /var/longhorn
